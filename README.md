# YouTube Insight Summarizer (Chrome Extension)

Расширение для Chrome в формате **Side Panel** (боковой панели), которое:
1. Забирает транскрипцию текущего видео YouTube.
2. Позволяет скопировать транскрипцию одной кнопкой.
3. Делает структурированное саммари + ключевые инсайты с эмодзи через ваш OpenAI API key.

## Установка

1. Откройте `chrome://extensions/`.
2. Включите **Developer mode**.
3. Нажмите **Load unpacked**.
4. Выберите папку проекта (`/workspace/codex_app`).

## Как пользоваться

1. Откройте нужное видео на YouTube (`youtube.com/watch?...`).
2. Нажмите на иконку расширения — откроется **Side Panel**.
3. Вставьте ваш OpenAI API key (`sk-...`) и, при необходимости, модель.
4. Нажмите **📄 Получить транскрипцию**.
5. При необходимости нажмите **📋 Скопировать транскрипцию**.
6. Нажмите **✨ Сделать саммари и инсайты**.

## Диагностика

- При открытии панели в консоли выводится `Popup loaded`.
- Если транскрипт не найден, в интерфейсе показывается: `Ошибка: транскрипт не найден`.

## Примечания

- Если у видео нет субтитров/транскрипции, извлечение не сработает.
- API key сохраняется в `chrome.storage.local` в браузере.
- По умолчанию используется `gpt-4o-mini`.
